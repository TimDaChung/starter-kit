# starter-kit 的 Python 第三方依賴。安裝：pip install -r requirements.txt
# Pillow：generate2dsprite / generate2dmap 的後製腳本、imagen 的 magenta 去背
# playwright：webapp-testing（裝完還要跑 python -m playwright install chromium）
# 生圖本身不需要額外套件：走 image-studio（主任另行發放），不在本 kit 的依賴範圍
#
# data-report-builder 生成的報表 skill 才需要，用到再裝：pip install pandas plotnine
# （不列成正式行，避免每個人都裝；plotnine 會一併帶入 matplotlib）
Pillow>=10
playwright>=1.45
