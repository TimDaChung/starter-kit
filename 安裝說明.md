# 手動安裝說明

推薦走 README 的「貼給 Claude」自動安裝;這份是手動備援與細節說明。

## 前置

- 裝了 git(沒有:`winget install --id Git.Git`)
- Claude Code 可正常使用
- **企劃 / 遊戲原型類 7 支不需要下面任何東西**;以下只影響生圖、測試、試玩迴圈:

| 依賴 | 影響的 skill | 沒有的話怎麼裝 |
|---|---|---|
| Python 3.10+ | imagen 系列 5 支、generate2dsprite、generate2dmap、playtest-loop、webapp-testing | `winget install Python.Python.3.12` |
| Pillow、playwright(pip) | generate2dsprite、generate2dmap、imagen 去背、webapp-testing | `pip install -r %USERPROFILE%\starter-kit\requirements.txt` |
| Playwright 的 Chromium | webapp-testing | `python -m playwright install chromium` |
| Node.js + chrome-devtools MCP | playtest-loop(讀玩家分頁)、game-develop 的視覺 QA | `winget install OpenJS.NodeJS.LTS`,再 `claude mcp add --scope user chrome-devtools -- cmd /c npx chrome-devtools-mcp@latest --browser-url http://127.0.0.1:9222` |
| Gemini API key | 生圖 5 支 | 見步驟 5 |

自動安裝的健檢會偵測缺哪些、列出來**問一次**要不要順手裝,同意才動你的 Python 環境與 MCP 設定。

### Chrome 開遠端偵錯(playtest-loop 要讀你玩的分頁才需要)

chrome-devtools MCP 是連到你自己開的 Chrome,所以 Chrome 要帶 `--remote-debugging-port=9222` 啟動。只改一次:

1. 桌面或工作列的 Chrome 捷徑按右鍵 → **內容**(工作列的要先右鍵 Chrome 圖示 → 再右鍵「Google Chrome」→ 內容)
2. **目標**欄位尾端(引號外)加一個空格再貼:`--remote-debugging-port=9222`
   改完長這樣:`"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222`
3. 確定 → **完全關閉 Chrome**(工作列右下角托盤若有 Chrome 圖示也要結束)→ 用這個捷徑重開
4. 驗證:瀏覽器開 `http://127.0.0.1:9222/json/version` 看到一段 JSON 就成功;看到「無法連線」代表 Chrome 沒真的關掉重開,或開的是別的捷徑

只有從這個捷徑啟動的 Chrome 才會開這個 port;從別的地方(開始選單、其他捷徑、預設瀏覽器連結)開起來的 Chrome 若比它先開,參數不會生效。不想改捷徑的人,playtest-loop 會退到「主控台跑 `exportDevNotes()` 貼回來」模式,功能照用只是多一步手動。

## 步驟

1. Clone:
   ```
   git clone https://github.com/TimDaChung/starter-kit.git %USERPROFILE%\starter-kit
   ```
2. 建 junction(要哪支裝哪支;PowerShell):
   ```powershell
   New-Item -ItemType Junction -Path "$env:USERPROFILE\.claude\skills\product-planning" -Target "$env:USERPROFILE\starter-kit\skills\product-planning"
   ```
   (其餘 skill 同格式,換名稱)
3. Agents 複製:`starter-kit\agents\*.md` → `%USERPROFILE%\.claude\agents\`(想要個人分身,另外把 `templates\my-voice.example.md` 複製成 `agents\my-voice.md` 並把 name 改回 `my-voice`)
4. 全域設定:
   - 沒有 `%USERPROFILE%\.claude\CLAUDE.md` → 把 `CLAUDE.starter.md` 拷過去改名 `CLAUDE.md`
   - 已有 → 請 Claude 讀兩份後,把你缺的規則直接併入(原內容不動);跟你既有規則衝突的會列出來讓你決定
5. 生圖要用的話:`skills\imagen\.env.example` 複製成 `.env`,到 https://aistudio.google.com/apikey 拿 key 填入
6. **重開 Claude Code**(新 agents 才會生效;skills 即時生效)

## 升級

對 Claude 說「starter 升級」或「新手包升級」;或手動 `git -C %USERPROFILE%\starter-kit pull`(junction 裝的 skills 立即生效,agents 有更新要重拷+重開)。

## 不會被升級動到的東西

你的 CLAUDE.md、imagen/.env、你自己的 skills/agents、secretary(它有自己的 kit)。

## 常見卡點

- mklink 報語法錯 → 用 PowerShell 的 New-Item(cmd 的 mklink 引號很挑)
- 新 agent 叫不動 → 沒重開 Claude Code
- 生圖報錯找不到 key → .env 沒建或 key 沒填
- `ModuleNotFoundError: No module named 'PIL'` / `'playwright'` → 沒跑 `pip install -r requirements.txt`;playwright 裝了還報找不到瀏覽器 → 沒跑 `python -m playwright install chromium`
- playtest-loop 說找不到分頁 / 讀不到筆記 → chrome-devtools MCP 沒接,或 Chrome 不是用 `--remote-debugging-port=9222` 啟動的(見前置的「Chrome 開遠端偵錯」;先開 `http://127.0.0.1:9222/json/version` 確認 port 有開)
